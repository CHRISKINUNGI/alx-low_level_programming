#include<stdio.h>

/**
  * add - adds two numbers
  *
  * @a: first numeber
  * @b: second number
  *
  * Return: Adds the two number and returns the sum
  */

int add(int a, int b)
{
	return (a + b);
}
